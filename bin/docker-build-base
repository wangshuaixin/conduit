#!/bin/sh

# Builds (or pulls) our base runtime docker image.

set -eu

. bin/_docker.sh

export tag="2017-10-30.01"

if (docker_pull base "${tag}" 1>&2); then
    echo "$(docker_repo base):${tag}"
else
    docker_build base "${tag}" Dockerfile-base
fi
