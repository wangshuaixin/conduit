#!/bin/sh

set -eu

. bin/_docker.sh
. bin/_tag.sh

dockerfile=proxy/Dockerfile

validate_proxy_deps_tag $dockerfile

bin/docker-build-base 1>&2
bin/docker-build-proxy-deps 1>&2

docker_build proxy "$(head_root_tag)" $dockerfile --build-arg="RELEASE=${PROXY_RELEASE:-1}"
