#!/bin/sh

set -eu

. bin/_docker.sh
. bin/_tag.sh

# Build gcr.io/runconduit/cli-bin, which is used by cli/Dockerfile.
bin/docker-build-cli-bin 1>&2

docker_build cli "$(head_root_tag)" cli/Dockerfile
